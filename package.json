{
  "name": "lecrev",
  "private": "true",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "license": "MIT",
  "type": "module",
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "^11.0.8",
      "onFail": "download"
    }
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "turbo": "^2.9.9"
  }
}
